<!DOCTYPE html>
<html>
<head>
    <title><%- project.name %> Details</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f3f3f3;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            flex-direction: column;
        }

        h1 {
            margin: 20px 0;
        }

        p {
            margin: 10px 0;
            padding: 0 20px;
        }

        .btn {
            display: inline-block;
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            text-decoration: none;
            border: none;
            border-radius: 3px;
            cursor: pointer;
        }

        .btn:hover {
            background-color: #0056b3;
        }

        .back-link {
            display: block;
            margin-top: 20px;
            color: #007bff;
            text-decoration: none;
        }

        .back-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <h1><%- project.name %> Details</h1>
    <p>Description: <%- project.description %></p>
<% if (isOwner) { %>
    <!-- Display edit options for the owner -->
    <a href="/projects/<%= project._id %>/edit" class="btn">Edit Project</a>
<% } %>

    <a href="/dashboard" class="back-link">Back to Dashboard</a>
</body>
</html>


